# Self-Ensemble Generation Requirements
# Python 3.10+ required

# =============================================================================
# PyTorch Installation (MUST INSTALL FIRST)
# =============================================================================
# For FlexAttention support (generate_flex_attention.py, generate_myriadlama.py):
#   pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu121
#
# For other scripts (can use stable PyTorch):
#   pip install torch torchvision torchaudio
#
# Note: FlexAttention requires PyTorch 2.5+ (nightly build)
# =============================================================================

# Core ML/NLP libraries
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
scikit-learn>=1.3.0

# NLP tools
spacy>=3.5.0
# After installing spacy, run: python -m spacy download en_core_web_lg

# Visualization
matplotlib>=3.7.0

# Progress bars and utilities
tqdm>=4.65.0

# File formats
feather-format

# Development and testing (optional)
jupyter>=1.0.0
ipykernel>=6.25.0

# =============================================================================
# Post-installation steps:
# =============================================================================
# 1. Download spacy model:
#    python -m spacy download en_core_web_lg
#
# 2. Verify FlexAttention availability (if using flex scripts):
#    python -c "from torch.nn.attention.flex_attention import flex_attention; print('✅ FlexAttention available')"
# =============================================================================

